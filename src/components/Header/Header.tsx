import React from 'react';

const Header = () => (
  <header className="d-flex justify-content-center p-1 bg-primary text-white">
    <h1>Client-server application</h1>
  </header>
);

export default Header;
